[build]
  publish = "public"
  command = "export PERL5LIB=/opt/buildhome/perl5/lib/perl5 && export PATH=/opt/buildhome/perl5/bin:$PATH && curl -L https://cpanmin.us | perl - App::cpanminus && cpanm Carton && carton install --deployment && carton exec perl Run.pm"
